# ⚡ Synaps Messenger

Synaps — це повноцінний веб-месенджер, що працює в реальному часі, створений на Flask та Socket.IO. Цей проєкт дозволяє користувачам реєструватися, знаходити інших користувачів та обмінюватися приватними повідомленнями, які зберігаються в базі даних.

**Важливо:** Цей проєкт наразі розміщено на безкоштовному плані Render, тому при першому заході може виникнути затримка ("холодний старт") до 60 секунд, поки сервер "прокидається".

</div>

---

## ✨ Основні Функції (Features)

* **Автентифікація користувачів:** Повна система реєстрації (`/register`) та входу (`/login`) з хешуванням паролів.
* **Приватні чати в реальному часі:** Спілкування один-на-один, реалізоване за допомогою кімнат (rooms) Socket.IO.
* **Список контактів:** Автоматично показує список усіх зареєстрованих користувачів.
* **Збереження історії:** Усі повідомлення зберігаються у базі даних PostgreSQL і завантажуються при відкритті чату.
* **Відображення часу:** Кожне повідомлення має позначку часу, що коректно відображається у часовому поясі користувача.
* **UX/UI:** Адаптивний інтерфейс зі стилізацією "мій/чужий", індикацією помилок та захистом від кириличних паролів.

---

## 🛠️ Технологічний Стек (Tech Stack)

### Бекенд (Back-End)
* **Python 3**
* **Flask:** Мікро-фреймворк для веб-сервера та маршрутизації (routes).
* **Flask-SocketIO:** Для двостороннього зв'язку в реальному часі (WebSockets).
* **Flask-SQLAlchemy:** ORM для роботи з базою даних.
* **Flask-Login:** Для керування сесіями користувачів.
* **Flask-WTF:** Для валідації форм.
* **PostgreSQL:** Продакшн-база даних (розміщена на Render).
* **Gunicorn & Eventlet:** WSGI-сервер для продакшену, здатний працювати з веб-сокетами.

### Фронтенд (Front-End)
* **HTML5** (з шаблонізатором **Jinja2**)
* **CSS3** (чистий CSS, інтегрований у HTML)
* **JavaScript (ES6+):** Клієнтська логіка для Socket.IO, керування DOM, зберігання історії чатів у кеші сесії.

---

## 🚀 Встановлення та Запуск (Setup)

Проєкт розроблено для деплою на PaaS (напр. Render), але його також можна запустити локально.

### 1. Клонування репозиторію

```bash
git clone [https://github.com/](https://github.com/)[TVIY_USERNAME]/Synaps-Messenger.git
cd Synaps-Messenger
